[package]
name = "smp-rs"
version = "0.1.0"
edition = "2021"
default-run = "smp-rs"

[dependencies]
argon2 = "0.5.3"
dotenvy = "0.15.7"
password-hash = { version = "0.5.0", features = ["getrandom"] }
vice = { git = "https://github.com/ariaandika/vice.git", version = "0.1.0" }
